function Tower (x, y, damage, fireRate, lvl) {
    this.x = x;
    this.y = y;
    this.lvl = lvl;
    this.damage = damage;
    this.fireRate = fireRate;

    this.show = () => {
        // based on the level selected damage and show style will change
    }

}